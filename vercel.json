{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "if [ ! -d .flutter-sdk ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 .flutter-sdk; fi && .flutter-sdk/bin/flutter config --no-analytics && .flutter-sdk/bin/flutter precache --web && cd app && ../.flutter-sdk/bin/flutter pub get",
  "buildCommand": "cd app && ../.flutter-sdk/bin/flutter build web --release --no-web-resources-cdn --dart-define=SUPABASE_URL=$SUPABASE_URL --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY --dart-define=PASSWORD_RESET_REDIRECT=${APP_URL:-https://$VERCEL_URL}",
  "outputDirectory": "app/build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
